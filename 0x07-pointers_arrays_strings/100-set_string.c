#include "main.h"

/**
 *set_string - sets values of a pointer to char
 *@s: The pointer to pointer
 *@to: The pointer to char
 * Return: Always 0
 */
void set_string(char **s, char *to)
{
	*s = to;
}

